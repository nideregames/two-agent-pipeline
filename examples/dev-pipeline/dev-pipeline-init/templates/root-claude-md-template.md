# CLAUDE.md — [PROJECT_NAME]

All project knowledge lives in the vault. This file is only a navigation pointer.

## Vault
[VAULT_PATH]

## Sub-projects

<!-- List ONLY directories that contain code or assets. Do NOT include vault/ itself. -->

| Name | Path | Subsystem |
|------|------|-----------|
| [DISPLAY_NAME] | `[DIR]/` | [SUBSYSTEM_NAME] |

## How to work

1. Read your spec from `[VAULT_PATH]/specs/`
2. Identify which sub-project it targets (check the `subsystem` field in spec frontmatter)
3. `cd` into that sub-project and read its `CLAUDE.md`
4. Follow the instructions there

If no spec — ask the user what to do.

<!-- DO NOT add anything else to this file. All project knowledge, constraints, conventions, and architecture live in the vault. This file is a thin router, not a knowledge store. -->
